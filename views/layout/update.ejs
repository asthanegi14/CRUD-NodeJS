<!DOCTYPE html>
<html lang="en">

<head>
       <meta charset="UTF-8">
       <meta name="viewport"
             content="width=device-width, initial-scale=1.0">
       <title>Document</title>
</head>

<body>
       <div class="container"
            style="width: fit-content; padding: 10% 0; justify-content: center; display: flex; flex-direction: column; align-items: center; height: screen;">
              <h1 class="heading">Update User (<%= user.name
                         %>)</h1>
              <form action="/update/<%= user._id %>"
                    method="post"
                    enctype="multipart/form-data">
                     <div class="mb-3">
                            <label for="name"
                                   class="form-label">Name</label>
                            <input type="text"
                                   class="form-control"
                                   id="name"
                                   name="name"
                                   value="<%= user.name %>">
                     </div>
                     <div class="mb-3">
                            <label for="technology"
                                   class="form-label">Technology</label>
                            <input type="text"
                                   class="form-control"
                                   id="technology"
                                   name="technology"
                                   value="<%= user.technology %>">
                     </div>
                     <div class="mb-3">
                            <label for="colors"
                                   class="form-label">Colors (comma-separated)</label>
                            <input type="text"
                                   class="form-control"
                                   id="colors"
                                   name="colors"
                                   value="<%= user.colors %>">
                     </div>
                     <div class="mb-3">
                            <label for="pricePerGram"
                                   class="form-label">Price Per Gram</label>
                            <input type="number"
                                   step="0.01"
                                   class="form-control"
                                   id="pricePerGram"
                                   name="pricePerGram"
                                   value="<%= user.pricePerGram %>">
                     </div>
                     <div class="mb-3">
                            <label for="applicationTypes"
                                   class="form-label">Application Types (comma-separated)</label>
                            <input type="text"
                                   class="form-control"
                                   id="applicationTypes"
                                   name="applicationTypes"
                                   value="<%= user.applicationTypes %>">
                     </div>
                     <div class="mb-3">
                            <label for="image"
                                   class="form-label">Upload Image</label>
                            <input type="file"
                                   class="form-control form-control-lg"
                                   id="image"
                                   name="image">
                            <img src="/<%= user.image %>"
                                 width="100"
                                 class="img-thumbnail mt-1">
                     </div>

                     <input type="hidden"
                            name="old_image"
                            value="<% user.image %>">
                     <button type="submit"
                             class="btn btn-success">Update User Details</button>
              </form>
       </div>
</body>

</html>